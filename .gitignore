node_modules/
dist/
config.json
workspace/
# Local runtime state — project registry and saved session transcripts.
projects.json
sessions/
*.log
.DS_Store
.env

# Local Claude Code settings (machine-specific; keep the shared CLAUDE.md spec)
.claude/settings.local.json
